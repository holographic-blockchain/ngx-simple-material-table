import { HbMatTableColumnHeaderPipe } from './hb-mat-table-column-header.pipe';

describe('HbMatTableColumnHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new HbMatTableColumnHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
